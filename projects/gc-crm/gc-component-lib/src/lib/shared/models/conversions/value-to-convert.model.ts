export interface GCCLIDataToConvert {
  unit_type: string;
  convert?: number;
  from: string;
  to: string;
  fraction_digits?: number;
  adjustment_type?: string;
  short_fraction_digits?: number;
}
