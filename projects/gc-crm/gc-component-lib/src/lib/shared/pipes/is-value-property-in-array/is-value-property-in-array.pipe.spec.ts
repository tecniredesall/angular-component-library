import { IsValuePropertyInArrayPipe } from './is-value-property-in-array.pipe';

describe('IsValuePropertyInArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new IsValuePropertyInArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
