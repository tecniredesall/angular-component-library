import { GcTranslatePipe } from './gc-translate.pipe';

describe('GcTranslatePipe', () => {
  it('create an instance', () => {
    const pipe = new GcTranslatePipe();
    expect(pipe).toBeTruthy();
  });
});
