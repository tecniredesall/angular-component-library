import { TableActiveColumnsKeyPipe } from './table-active-columns-key.pipe';

describe('TableActiveColumnsKeyPipe', () => {
  it('create an instance', () => {
    const pipe = new TableActiveColumnsKeyPipe();
    expect(pipe).toBeTruthy();
  });
});
